<%
var s='';

if( background_id && background.url && background_visible)
  s="background: url('/assets/backgrounds/" + background.url +"');";

if(background_color)
  s+='background-color:' + background_color + ';'

if( s!='' ) s=' style="' + s + '"';
%>
<%= s %>
