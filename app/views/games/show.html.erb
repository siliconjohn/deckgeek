<div class="game-show">
  <script type="text/javascript">
    window.App = window.App || {};
    window.App.data = window.App.data || {};
    App.data.game_id = <%= @game.id %>;

    $(function(){
      getGameHero('.game-show',<%= @game.to_json.to_s.html_safe %>);
      getDecks('.game-show',<%=  @decks.to_json.to_s.html_safe %>);
    });
  </script>
</div>
